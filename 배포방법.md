# 배포방법

- 프로젝트: DeveloperKit
- 프로젝트 유형: 문서·PowerShell 도구 저장소
- DeveloperKit: 2.0.0

## 자동 버전 정책

- 배포 버전의 기준은 Git 태그 `vMAJOR.MINOR.PATCH`로 한다.
- 태그와 `VERSION` 값은 일치해야 한다.
- 배포 전 PowerShell 구문 검사와 임시 프로젝트 적용·점검 테스트를 수행한다.
- 이 저장소는 제품 바이너리를 빌드·게시하지 않으며 프로젝트 파일을 자동 변경하지 않는다.

## 배포

1. `VERSION`, README, 변경 기록의 버전이 일치하는지 확인한다.
2. 적용·점검 도구 테스트와 `git diff --check`를 통과시킨다.
3. 사용자 요청과 승인 후 변경을 Commit한다.
4. 사용자 요청과 승인 후 `vMAJOR.MINOR.PATCH` 태그와 원격 저장소에 Push한다.

## 복구

- 이전 Git 태그의 문서와 도구를 사용한다.
- 대상 프로젝트에 이미 생성된 기존 파일은 DeveloperKit 재적용으로 덮어쓰지 않는다.
